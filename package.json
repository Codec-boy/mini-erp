{
  "name": "mini-erp-crm-portal",
  "version": "1.0.0",
  "description": "Mini ERP + CRM Operations Portal Monorepo",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build",
    "start": "cd server && npx prisma db push && npm start",
    "seed": "cd server && npm run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
